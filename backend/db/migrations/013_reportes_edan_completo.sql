-- Esquema completo reportes_edan (misma definición que ensureSchema y proteccion_civil_carabobo.sql).
-- En bases existentes, preferir reiniciar el backend (ensureSchema) o ejecutar los ALTER
-- de columnas faltantes que aplica el código, salvo que quieras recrear la tabla con respaldo.

USE proteccion_civil_carabobo;
